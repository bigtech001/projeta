kind = "web"
previewPath = "/"
title = "Projeto live"
version = "1.0.0"
id = "artifacts/church-projection"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20976

[services.development]
run = "pnpm --filter @workspace/church-projection run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/church-projection", "run", "build" ]
publicDir = "artifacts/church-projection/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20976"
BASE_PATH = "/"
